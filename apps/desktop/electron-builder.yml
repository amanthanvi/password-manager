appId: com.npw.passwordmanager
productName: npw
artifactName: npw-desktop-${version}-${os}-${arch}.${ext}

directories:
  output: dist-packages

files:
  - dist/**/*
  - dist-electron/**/*
  - native/**/*
  - package.json

asar: true
asarUnpack:
  - native/**/*

npmRebuild: false

mac:
  target:
    - dmg

win:
  target:
    - nsis

linux:
  target:
    - AppImage
    - deb
